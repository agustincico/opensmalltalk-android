<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Only for exporting fileouts to Downloads on Android 9 and older;
         Android 10+ uses MediaStore, which needs no permission. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="28" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name">
        <service
            android:name="au.com.darkside.x11server.XServerService"
            android:exported="false"
            android:stopWithTask="false" />

        <activity
            android:name="au.com.darkside.x11server.XServerActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="user"
            android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|screenLayout|fontScale|uiMode|orientation|screenSize|smallestScreenSize|layoutDirection|density"
            android:theme="@style/Theme.MaterialComponents.Light.DarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="x11" />
            </intent-filter>
        </activity>
        <activity
            android:name="au.com.darkside.x11server.AccessControlEditor"
            android:exported="false"
            android:label="Authorized IP addresses" />
        <!-- Tiny trampoline that lives in its OWN process (:restart) so it can
             kill + relaunch the main app process when switching images. See
             RestartActivity for why the old go-HOME + AlarmManager approach
             failed on Android 10+ (background-activity-start is blocked). -->
        <activity
            android:name="au.com.darkside.x11server.RestartActivity"
            android:process=":restart"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
    </application>
</manifest>